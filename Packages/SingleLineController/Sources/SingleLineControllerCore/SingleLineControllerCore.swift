// MIT License — Copyright (c) 2018-2026 Open Zesame
//
// SingleLineControllerCore — value-type primitives for the SLC architecture.
// File-moves into this module land in Phase 1 of the extraction plan.

/// Module-version sentinel. Allows `import SingleLineControllerCore` to succeed
/// during Phase 0 (skeleton) before any real types have been migrated.
public enum SingleLineControllerCore {
    /// Semantic version of the `SingleLineControllerCore` module. Bumped on
    /// breaking API changes once the package is consumed by external repos.
    public static let version = "0.0.1"
}
