// MIT License — Copyright (c) 2018-2026 Open Zesame
//
// SingleLineControllerController (UIKit) — SceneController, BarButton plumbing,
// nav-bar layout. File-moves into this module land in Phase 5 of the
// extraction plan.

/// Module-version sentinel. Allows `import SingleLineControllerController` to
/// succeed during Phase 0 (skeleton) before any real types have been migrated.
public enum SingleLineControllerController {
    /// Semantic version of the `SingleLineControllerController` module.
    public static let version = "0.0.1"
}
